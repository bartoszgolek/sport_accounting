{% extends 'base.html.twig' %}

{% block body %}
    <h1>Booking\Invoice creation</h1>

    <div class="col-md-3 col-md-offset-3">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
        {{ form_end(form) }}
    </div>
{% endblock %}
